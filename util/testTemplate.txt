import SolutionBase from '../../src/core/solutionBase';
import { Day__DAY_TWO_LETTER_NUMBER__ } from '../../src/solutions';

describe('Day__DAY_TWO_LETTER_NUMBER__ solution', function () {
    let solution: SolutionBase;

    describe('for input 1', function () {
        const input = ``;
        solution = new Day__DAY_TWO_LETTER_NUMBER__().init(input);

        it('solves part 1', async function () {
            const expected = 'Day__DAY_TWO_LETTER_NUMBER__Part1';
            expect(await solution.part1Async()).toEqual(expected);
        });

        it('solves part 2', async function () {
            const expected = 'Day__DAY_TWO_LETTER_NUMBER__Part2';
            expect(await solution.part2Async()).toEqual(expected);
        });
    });
});
